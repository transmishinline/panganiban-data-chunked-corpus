Thus, if the RFLP technique is like using a magnet to find the location of a needle in a haystack, PCR is like using a metal detector to determine whether a particular kind of needle is present, then copying it many times. This method is used where only minute specimens are found. The reaction enables a single strand of DNA to double immediately, and the process can be repeated several times.
